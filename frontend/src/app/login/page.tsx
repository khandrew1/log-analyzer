import Login from "@/components/user/login";

const Page = () => {
  return (
    <div className="flex h-full w-full items-center justify-center">
      <Login />
    </div>
  );
};

export default Page;
